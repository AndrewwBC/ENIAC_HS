[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "eniac_hs"
version = "0.1.0"
requires-python = ">=3.9"

[tool.setuptools]
py-modules = ["base"]

[tool.setuptools.packages.find]
where = ["."]
